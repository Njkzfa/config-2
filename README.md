# Инструмент визуализации графа зависимостей для pip-пакетов

Вариант №21  
Дисциплина: Конфигурационное управление  
Группа: ИКБО‑21‑24  

Что делает инструмент  
CLI‑утилита строит и анализирует граф зависимостей пакетов python(pip) без использования готовых средств менеджера пакетов и сторонних библиотек получения зависимостей.  

Этап 1. Параметры из опций командной строки; печать всех параметров (ключ‑значение); версия пакета; имя сгенерированого файла с изображением.  
Этап 2. Получение прямых зависимостей пакета из Python (pip) выбранного репозитория (URL/путь) и вывод на экран.  
Этап 3. Построение графа DFS c рекурсией, корректная обработка циклов; поддержан тестовый режим (файл с графом, имена пакетов — большие латинские буквы).  
Этап 4. Вывод порядка загрузки (PySocks; certifi, chardet и др.).  
Этап 5. Генерация текста диаграммы на языке Mermaid; сохранить файл в формате SVG.

## Файлы

- `main.py` — основной скрипт CLI.
- `config.xml` — пример конфигурационного файла.
- `test_repo.txt` — пример тестового репозитория для режима `test_mode=true`.
  
# Пример тестового репозитория
A: B C  
B: C  
C:  
D: A C  
  
## Примеры запуска

1. Вывести параметры конфигурации (Этап 1):

```bash
python main.py config.xml --mode print-config
```
<img width="1195" height="187" alt="image" src="https://github.com/user-attachments/assets/00530bb7-6784-4bcf-9e41-818626f47ef2" />


2. Вывести прямые зависимости пакета (Этап 2):

```bash
python main.py config.xml --mode direct-deps
```
<img width="1203" height="218" alt="image" src="https://github.com/user-attachments/assets/933f5714-1844-4e5e-b06b-3dce6eee1953" />


3. Построить и вывести граф зависимостей (Этап 3):

```bash
python main.py config.xml --mode graph
```


4. Вывести порядок загрузки зависимостей (Этап 4):

```bash
python main.py config.xml --mode load-order
```


5. Сгенерировать Mermaid и SVG (Этап 5):

```bash
python main.py config.xml --mode visualize
```


Для генерации SVG требуется установленный `mmdc` (mermaid-cli).


